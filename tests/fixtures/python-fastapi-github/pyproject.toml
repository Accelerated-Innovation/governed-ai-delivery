[project]
name = "fixture-python-fastapi"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn",
    "pydantic",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-bdd",
    "httpx",
]
