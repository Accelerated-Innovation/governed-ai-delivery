---
name: govkit-application-governance
description: Work from accepted project policy and contracts.
---

# Application Governance

Read the accepted `.govkit/profile.yaml` and its referenced contracts. Treat pack examples and detected code as advisory context. Record unresolved policy decisions before changing required controls. Do not infer accepted architecture from an exemplar.

Read [reference](references/guide.md). Use `govkit pack preview` and `govkit pack verify` to inspect the selected resources and controls. If only the source was copied with legacy `extension add`, complete explicit profile/pack setup before invoking a locked control.
