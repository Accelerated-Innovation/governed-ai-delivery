# Gherkin Delivery

Use Gherkin scenarios when accepted workflow policy calls for them.

This pack supplies advisory guidance. Installing it does not accept its examples as project policy. Select it explicitly in the desired profile; native skill copies reference the pinned pack resources.

Legacy `extension add` can copy this source and guidance, but it does not create a declarative lock or accept this capability as project policy. Use explicit profile/pack setup for locked controls.
