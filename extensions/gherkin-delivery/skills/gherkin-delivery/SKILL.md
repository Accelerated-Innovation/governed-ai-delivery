---
name: gherkin-delivery
description: Use Gherkin scenarios when accepted workflow policy calls for them.
---

# Gherkin Delivery

Read the accepted project workflow and feature intent. Write observable Given/When/Then scenarios and demonstrate a failing scenario before implementation. Use this workflow only where the accepted policy permits it. This skill does not install or require LLM evaluation.

Read [reference](references/guide.md). Use `govkit pack preview` and `govkit pack verify` to inspect the selected resources and controls. If only the source was copied with legacy `extension add`, complete explicit profile/pack setup before invoking a locked control.
