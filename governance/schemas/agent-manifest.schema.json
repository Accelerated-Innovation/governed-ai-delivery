{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent Manifest Schema",
  "description": "Schema for agent manifest.json files in agents/<agent-name>/manifest.json",
  "type": "object",
  "required": ["agent", "description", "variants"],
  "additionalProperties": false,
  "properties": {
    "agent": {
      "type": "string",
      "minLength": 1,
      "description": "Agent identifier (e.g., 'claude-code', 'copilot')"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the agent"
    },
    "options": {
      "type": "object",
      "description": "Variant options presented to the user during govkit apply",
      "additionalProperties": {
        "$ref": "#/$defs/option"
      }
    },
    "variants": {
      "type": "object",
      "description": "Variant dimensions (type, ui, ci) mapping option values to file lists",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "$ref": "#/$defs/variant_config"
        }
      }
    }
  },
  "$defs": {
    "option": {
      "type": "object",
      "required": ["prompt", "choices"],
      "additionalProperties": false,
      "properties": {
        "prompt": {
          "type": "string",
          "description": "Interactive prompt text shown to the user"
        },
        "choices": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "Valid choices for this option"
        },
        "default": {
          "type": "string",
          "description": "Default value if user presses enter without choosing"
        }
      }
    },
    "file_entry": {
      "type": "object",
      "required": ["src", "dest"],
      "additionalProperties": false,
      "properties": {
        "src": {
          "type": "string",
          "description": "Source path relative to the agent directory"
        },
        "dest": {
          "type": "string",
          "description": "Destination path relative to the target project root"
        }
      }
    },
    "variant_config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "files": {
          "type": "array",
          "items": { "$ref": "#/$defs/file_entry" },
          "description": "Agent-specific files to copy (src relative to agent dir, dest relative to target)"
        },
        "shared": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Shared governance paths to copy from repo root to target (skip-existing on apply)"
        },
        "governed": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Govkit-owned governed contracts to copy from repo root to target (refreshed on upgrade)"
        },
        "level_3": {
          "$ref": "#/$defs/level_override",
          "description": "DEPRECATED in v0.7.0 — kept transitionally during the L3/L4 swap. Will be removed once all manifests migrate to top-level (L3) + level_4 add-on."
        },
        "level_4": {
          "$ref": "#/$defs/level_override",
          "description": "Spec-Driven Add-On overlay applied at maturity level 4 (default mode: merge)"
        },
        "level_5": {
          "$ref": "#/$defs/level_override",
          "description": "GenAI Operations overlay applied at maturity level 5 (default mode: replace)"
        }
      }
    },
    "level_override": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["merge", "replace"],
          "description": "Override application mode. 'merge' (default for level_4) appends entries to the base, with dest-collision resolution preferring the override. 'replace' (default for level_5) discards the base and uses only this block's entries."
        },
        "files": {
          "type": "array",
          "items": { "$ref": "#/$defs/file_entry" },
          "description": "Level-specific agent files. Behavior depends on 'mode'."
        },
        "shared": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Level-specific shared governance paths. Behavior depends on 'mode'."
        },
        "governed": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Level-specific govkit-owned governed contracts. Behavior depends on 'mode'."
        }
      }
    }
  }
}
